version: '3.9'
services:
  upimage:
    image: devecor/upimage:2.1.6
    ports:
      - '8080:8080'
    volumes:
      - ~/upimage-home:/root/upimage-home
    environment:
      upimage_host: http://localhost:8080
